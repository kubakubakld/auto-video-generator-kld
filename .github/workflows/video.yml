name: Generate Video

on:
  workflow_dispatch:
    inputs:
      text:
        description: "Text for the video"
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Generate video
      run: python main.py "${{ github.event.inputs.text }}"

    - name: Upload video
      uses: actions/upload-artifact@v3
      with:
        name: output-video
        path: output.mp4
